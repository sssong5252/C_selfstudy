#include <stdio.h>
// int main(void) {

    // char data = 'a';
    // char key = 0xff;
    // char encrpted_data, orig_data;

    // printf("원래의 문자 = %c\n", data);

    // encrpted_data = data ^ key;
    // printf("암호화된 문자 = %c\n", encrpted_data);

    // orig_data = encrpted_data ^ key;
    // printf("복원된 문자 = %c\n", orig_data);

    // int i;
    // double f;

    // f = 5 / 4;

    // printf("%f\n", f);

    // f = (double)5 / 4;
    // printf("%f\n", f);

    // f = 5.0 / 4;
    // printf("%f\n", f);

    // f = (double)5 / (double)4;
    // printf("%f\n", f);

    // i = 1.3 + 1.8;
    // printf("%d\n", i);

    // i = (int)1.3 +  (int)1.8;

    // printf("%d\n", i);

    // int x = 0, y = 0;
    // int result;

    // result = 2 > 3 || 6 > 7;
    // printf("%d\n", result);

    // result = 2 || 3 && 3 > 2;
    // printf("%d\n", result);

    // result = x = y = 1;
    // printf("%d\n", result);

    // result = - ++x + y--;
    // printf("%d\n", result);

    // double f_temp;
    // double c_temp;

    // printf("화씨온도를 입력하시오: ");
    // scanf("%lf", &f_temp);
    // c_temp = 5.0 / 9.0 * (f_temp - 32);
    // printf("섭씨온도는 %f 입니다.", c_temp);

    // int num;

    // printf("정수를 입력하세요: ");
    // scanf("%d", &num);

    // if(num > 0)
    //     printf("양수입니다\n");
    
    // else if(num < 0)
    //     printf("음수입니다\n");
    // else {
    //     printf("0입니다\n");
    // }
        
    // printf("입력된 값은 %d입니다", num);

    // int number;

    // printf("정수를 입력하세요");
    // scanf("%d", &number);

    // if(number % 2 == 0)
    //     printf("입력된 정수는 짝수입니다\n");
    // else
    //     printf("입력된 정수는 홀수입니다.");
        
    // int year;

    // printf("연도를 입력하세요: ");
    // scanf("%d", &year);

    // if((year % 4 == 0 &&))

    // char ch;
    // printf("문자를 입력하세요: ");

    // ch = getchar()
    // if(ch >= 'A' %% ch<='Z')
    //     printf("%c는 대문자입니다", ch);

    // if(ch)

    // int x, y, z;
    // char r, t, e;

    // printf("방정식의 계수를 차수가 높은 순으로 입력하시오: ");
    // scanf("%d, %d, %d", &x, &y, &z);

    // r = 근의 합;
    
    // printf("구하고 싶은 것은 무엇입니까? : ");
    // scanf("")
    // if (x == 0 && y == 0 && z == 0);
    //     printf("방정식이 존재하지 않습니다.")

    // char op;
    // int x, y, result;

    // printf("수식을 입력하세요");
    // scanf("%d %c %d", &x, &op, &y);
    // int x, y, r;

    // printf("두개의 정수를 입력하시오(큰 수부터 오름차순으로): ");
    // scanf("%d%d", &x, &y);

    // while(y != 0) {
    //     r = x % y;
    //     x = y;
    //     y = r;
    // }
    // printf("최대공약수는 %d 입니다", x);
    
    // int num, sum;

    // do {
    //     printf("정수를 입력하세요: ");
    //     scanf("%d", &num);
    //     sum += num; 
    // } while (num != 0);

    // printf(" 숫자들의 합 = %d\n", sum);

    // int i = 0;
    // printf("정덥을 추측해보자: ")

    // do{
    // }

    // int x, y;
    // for(y = 1; y <= 5; y++) {
    //     for(x = 0; x < y; x++)
    //         printf("*");
    //     printf("\n");
    // }

    // // for (i = 0; i < 5; i++)
    // //     printf("hello world");
    // int a, b, c;
    // for(a = 1; a <= 100; a++){
    //     for(b = 1; b <= 100, b++) { 
    //         for(c = 1; c <= 100, c++){ 
    //             if((a*a + b*b) == c*c);
    //                 printf("%d%d%d\n",a, b, c);
    //         }
    //     }
    // }
int main(void) {
    // char letter;

    // while(1) {
    //     printf("소문자를 입력하세요: ");
    //     scanf("%c", &letter);

    //     if(letter == 'Q')
    //         break;
    //     if(letter < 'a' || letter > 'z')
    //         continue;

    //     letter -= 32;
    //     printf("변환된 대문자는 %c입니다.\n ", letter);
    // }
    // double divisor, divident, sum;
    // int loop_count;

    // divisor = 1.0;
    // divident = 4.0;5
    // sum = 0.0;
    // printf("반복횟수: ");
    // scanf("%d", &loop_count);

    // while (loop_count > 0)
    // {
    //     sum = sum + divident / divisor;
    //     divident = -1.0 * divident;
    //     divisor = divisor + 2;
    //     loop_count --;
    // }
    // printf("Pi = %f", sum);

    
    
    return 0;
}

